<div class="row justify-content-center align-content-center">
    <div id="alertContainer">
        @foreach (var message in alerts)
        {
            <Alert Message="message"></Alert>
        }
    </div>
</div>
